services:
  wolfrelay:
    build: .
    container_name: wolfrelay
    restart: unless-stopped
    ports:
      - "8911:8911"
    environment:
      PORT: "8911"
      FLARESOLVERR_URL: "http://host.docker.internal:8191/v1"
    extra_hosts:
      - "host.docker.internal:host-gateway"
